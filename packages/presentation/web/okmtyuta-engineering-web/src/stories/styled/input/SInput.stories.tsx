import { SInput, SInputProps } from '../../../components/styled/input';

export default {
  title: 'components/input/SInput',
  component: SInput,
};

export const Primary = (args: SInputProps) => <SInput {...args} />;
